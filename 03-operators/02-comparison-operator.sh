#!/bin/bash
a=5
b=10
if [ $a -lt $b ]; then
    echo "$a is less than $b"
fi

# read -p "Enter value of a = " a
# read -p "Enter value of b = " b

# if [ "$a" -lt "$b" ]; then
#     echo "$a is less than $b"
# else
#     echo "$a is greater than $b"
# fi

# Or
# a=input("a = ")
# b=input("b = ")

# if [ "$a" -lt "$b" ]; then
#     echo "$a is less than $b"
# else
#     echo "$a is greater than $b"
# fi

